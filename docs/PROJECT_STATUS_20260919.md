# PKAS 项目状态快照 · 2026-09-19

## 本次同步

- 本机完整工作目录：`E:\codex-kb`。
- GitHub `main` 当前已同步到 `a0e9395`，作为已验证源码基线。
- 本次没有新的源码功能改动；新增本文件作为今天的真实状态记录，避免制造空提交。

## 已同步到 GitHub 的内容

- PKAS Python 核心、知识底座、文件接入、解析、全文检索、混合检索和向量索引代码；
- MCP 接口、Codex 配置适配、WeFlow 同步代码和运行脚本；
- 网页控制台、桌面壳源码、配置模板和自动化测试；
- RAG 评测脚本与当前项目说明。

## 留在本机的内容

- SQLite 数据库、Qdrant 向量数据、微信聊天记录、原始资料、运行报告和备份；
- `.env`、API 密钥、个人路径和本机运行状态；
- 尚未经过发布筛选的工作树文件。

## 当前验证边界

- 上一版发布快照验证：345 项测试通过，1 项环境测试跳过，网页生产构建通过；
- 基础 Python 编译和错误级 Ruff 检查通过；
- 完整 Ruff 仍有风格类告警，不能表述为全量代码规范已清零；
- 全盘 AI 速判、实际 EXE 用户验收和 MCP 真实客户端体验仍属于本机后续工作，不因源码同步而自动视为完成。

这份文件只记录项目状态，不包含个人资料、聊天正文、数据库内容或密钥。
