# 本地搜索与两份真实文档补入库

2026-09-08，范围 E:/codex-kb。未删除Agent/工作流，未扩大同步根或启动整盘扫描。

## 已验证效果

- “资料底座”首部新增本地文件/微信搜索。调用 `/api/foundation/search`。
- 新入口不混入codex-turn任务请求；原API/MCP默认行为保持兼容，未宣称全局排序已修复。
- 聊天可显式包含待归类和受限资料；不更改客户身份、数据隐私分类或云端处理权限。
- 直接调用SQLite/FTS读接口，不进入向量、重排或模型；按通道返回结果/错误/范围提示。
- 真实页面输入“7月25日修改”得到1条PDF结果及page:6/block:2来源定位；
  明确勾选聊天和受限资料后，“项目”返回10条聊天结果，均有来源引用。
- 30项聚焦测试通过；Ruff、前端构建通过。实际本机页面验证脚本为
  reports/local-search-browser-check.cjs；文件检索截图local-search-files-20260908.png。

## 两份既有目录资料补入库

从已登记启用的 E:/wordspace 目录，明确限定以下台账ID，没有重新遍历目录：

| 台账ID | 文档 | 新来源ID | 新切片 |
|---|---|---|---|
| sni_ed31279f79a24c47854afca0e670b032 | 0730星八课平台功能需求确认表.docx | src_e5c316afb4ec40ec9295d8130a0a384e | 1 |
| sni_c6325cfe8bb94dab8e767c7e87a18de1 | 7月25日修改.pdf | src_624dc5fbc88e496cbb3bdffcc3387364 | 3 |

两份原文件导入前后SHA256一致，未修改源项目。新增原件快照沿用hash去重存储。
新来源哈希分别1232dc90924dca3bb292bcd28214d678a19a6f71d5840c9a50494c635335a47d、
fd43aeea99bf78e02732d9ffb40e5ad714e0ba61cb9c0e4a4fe8616f80b2adbb。
源文件内容仅本地原生解析；凭据模式预检发现疑似凭据时会停止。
不存在“简单预检可发现全部秘密”的保证。

“7月25日修改”从0结果到1结果；另一文档导入前关键词曾命中2条其他来源，
导入后精确匹配到原文件1条，不能把之前的2条称作该原件已可搜。
全文已可搜不代表PDF图片内容已完整识别。向量触发器写入待处理事件，未运行Worker。

## 边界和后续

- `catalog_import.import_catalog_items`支持明确确认的1至10份台账文件，限制格式、10MB、目录和链接边界。
- 本次操作脚本reports/import-catalog-pilot.py有备份行比对，已执行后不可直接重复生产运行。
- 尚未提供普通用户批量导入按钮/任务取消恢复；范围确认整盘接入仍待实施。
- 本次只新增本地搜索入口；统一混合检索、MCP默认范围以及人工质量评测尚未全部完善。

## 恢复

I:/PKAS-backups/foundation-search-20260908：源码/web-dist哈希核对，一致性主库备份
2,888,601,600字节，quick_check=ok。选定两行导入前与备份逐字段比较一致。
SQLite事务写入sources/documents/chunks/blocks/FTS/audit，触发器登记outbox；
后续链接sync_items。恢复时必须保护之后合法新增记录，不能直接覆盖当前全库。
新原件副本位于data/raw/sha256/12及fd下对应上述hash文件；原件保留。
