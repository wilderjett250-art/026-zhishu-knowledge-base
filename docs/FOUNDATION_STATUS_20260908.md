# 资料底座状态管理 · 2026-09-08

## 本次已交付

本机 EXE 复用的网页新增“资料底座”（/?page=foundation）。四个独立只读接口：
`/api/foundation/overview`、`documents`、`files`、`scopes`。

- 已接入目录、登记模式、上次扫描时间与历史台账分别展示。只登记不代表内容可搜。
- 按既有资料源和状态分页查看文件；无可索引文字独立标记为需要解析检查，而非成功。
- 有效非Codex文档逐份对账切片、全文索引、向量登记数量和新版切片数。
- 不把缺少向量登记当成已排队，不把登记数量当成在线服务或新鲜度验证。
- 本机固定盘和重定向后的用户常用目录作为建议展示，尚未授权、扫描或注册。
- SQLite mode=ro + query_only，8秒SQL预算；前端12秒超时、重试和请求取消，独立加载。
- 原文、联系人、密钥、模型输出不进入新增统计；状态接口自查不计入使用率。

## 验证

- 17项 foundation/API/runtime 聚焦测试通过，隔离数据位于 I:/PKAS-test-runs。
- Ruff、前端TypeScript/Vite构建通过。
- 真实本机API/Edge无头页面完成加载、筛选、分页、模拟503和恢复重试；无其他页面的批量API请求。
- `reports/foundation-browser-check.cjs` 为可重放检查；截图 `reports/foundation-live-20260908.png`。
- 只读本机初测：台账2.595秒，50份文档对账3.533秒；不是P95保证。

## 尚未完成（不因页面存在而称完成）

1. 整盘选择、排除预览、确认注册：旧SyncService仍禁止磁盘根；需新增有界接入和链接/系统目录排除测试。
2. 从cataloged资料选择一批解析与索引、队列取消/重试、模型费用确认。
3. 微信本地授权搜索与云端权限分离；任务记录的搜索权重与入库授权。
4. 旧切片迁移、解析遗漏验证、失效原路径重定位、向量新鲜度和在线查询验收。
5. 真实人工评测仍未完成。此次未新增真实资料、未全库向量化、未启用同步或Agent。

## 恢复

I:/PKAS-backups/foundation-status-20260908：受影响源码与web/dist逐文件哈希核验；
pkas.sqlite使用SQLite一致性backup，2,888,007,680字节，quick_check=ok。
备份保留，不清理旧恢复点。范围及还原路径见RESTORE.md。
API在核对原进程命令后无窗口重启；未启动Qdrant/Core/WeFlow/计划任务。
